import React from 'react';
import styled from 'styled-components';

const ImageDiff: React.FC = () => {
  return <Wrapper>ImageDiff</Wrapper>;
};

export default ImageDiff;

const Wrapper = styled.div``;
