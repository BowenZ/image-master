import React from 'react';
import styled from 'styled-components';

const ImageCrop: React.FC = () => {
  return <Wrapper>ImageCrop</Wrapper>;
};

export default ImageCrop;

const Wrapper = styled.div``;
